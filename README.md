# DATA_MINING_V2.0
```
python -m venv venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass (Nếu activate venv lỗi)
venv\Scripts\activate
pip install -r requirements.txt
python src/scripts/run_pipeline.py  | python -m scripts.run_pipeline
python src/scripts/run_papermill.py | python -m scripts.run_papermill
streamlit run app/app.py            | python -m streamlit run app/app.py
```
